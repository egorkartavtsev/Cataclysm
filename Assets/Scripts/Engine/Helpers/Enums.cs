public enum BackgroundType
{ 
    Water,
    Sand,
    Ground
}
public enum ResItemType
{ 
    Resource,
    Material,
    Product
}

public enum ResRarityType
{ 
    None,
    Common,
    Rare,
    Unique
}

public enum LocationObjectType
{ 
    Resource,
    Building,
    Decoration
}

public enum GameMode
{ 
    DefaultView,
    BuildingView,
    InMenu
}

public enum SceneType
{ 
    Prod,
    TestPlayer
}